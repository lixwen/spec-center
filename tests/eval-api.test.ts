/**
 * TODO: Next.js API route integration tests for /api/eval/* require a full app test harness
 * (or MSW + route handlers). Skipped until that setup exists.
 */

import { describe, it } from "vitest";

describe.skip("eval API routes", () => {
  it("placeholder", () => {
    /* intentionally empty */
  });
});
